# Session Handoff - 2026-04-04

## Latest Summary
- Memory is now treated as a top priority.
- `PROJECT_MEMORY.md` is the local resume file for new sessions.
- Obsidian session logging is mandatory per Codex Workflow.
- `codex-workflow-v1/` is present locally and must remain untracked.
- Canonical scaffolding added: `.agent/`, plus root `directives/` and `execution/`.

## Current Risks / Gaps
- Canonical `.agent/` structure is not yet fully populated with real directives or execution scripts.
- Memory hook files were rebuilt today; older context is not included.

## Next Steps
- Populate `directives/` with real SOPs as they stabilize.
- Add deterministic scripts to `execution/` as tooling is created.
